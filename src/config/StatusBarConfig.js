import {StatusBar} from 'react-native';

//Altera o background onde fica os icones (bateria, wifi, rede) 
// Parece que só funciona para Android
StatusBar.setBackgroundColor('#DA552F');

//Altera a cor dos icones (bateria, wifi, rede)
StatusBar.setBarStyle('light-content');